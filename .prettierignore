# Vendored Hex sources — `deps/live_vue/assets/*.ts` and friends are a dependency's code, not ours.
# (Same reason `bin/typecheck.mjs` reports but does not gate on `deps/` diagnostics.)
deps/

# Build output and installed packages.
_build/
node_modules/
assets/node_modules/
priv/static/

# Generated by the Nuxt UI / unplugin Vite plugins on every build — reformatting them is churn that
# the next `mix assets.build` reverts.
assets/auto-imports.d.ts
assets/components.d.ts
